# BrandPulse TODOS

Deferred items from the Unified Intelligence Dashboard (Approach B) CEO plan review.
Last updated: 2026-05-24 | Branch: main

---

## Phase 2: Push Delivery (E1)

**Trigger:** If CEO logs into the dashboard < 3 times in the first month after launch.

Weekly digest text is already generated by Approach B. Push delivery is ~30min CC once triggered.

- [ ] Evaluate CEO login frequency after 4 weeks
- [ ] If < 3 logins: implement WhatsApp push via CallMeBot or Twilio WhatsApp API
- [ ] Email fallback: SendGrid transactional email with digest as plain text
- **Prerequisite:** WhatsApp Business API approval (submit application early — approval takes 1-4 weeks)

---

## Phase 2: PDF / Board Report Export (E3)

**Trigger:** After dashboard data has been validated as correct for ≥4 weeks.

One-tap board-ready PDF of the weekly intelligence digest + branch health scores + Commercial Performance table.

- [ ] Validate dashboard data is accurate for 4 consecutive weeks
- [ ] Implement: Flask endpoint `/t/<slug>/report/pdf` using WeasyPrint or headless Chromium
- [ ] Layout: BrandPulse header + Hakshan logo + digest paragraph + branch table + health badges
- **Note:** A polished PDF of wrong data is worse than no PDF. Do not implement until validation is complete.

---

## Phase 2: Operating Costs & Marketing Expenses

**Trigger:** After FeedMe sales data is stable and validated.

CEO originally requested operating costs and marketing expenses as part of the dashboard. These are deferred until the sales data foundation is solid.

- [ ] Identify data source for operating costs (manual upload? accounting software API?)
- [ ] Identify data source for marketing spend (Facebook/Instagram Ads API? manual?)
- [ ] Design schema: `operating_costs` table, `marketing_spend` table
- [ ] Add "Cost vs Revenue" section to Commercial Performance tab
- [ ] ROI calculation: revenue per marketing dollar per branch

---

## Phase 2: Multi-Tenant Scale

**Trigger:** When second paying tenant onboards (Bai Wei or next customer).

Current `daily_snapshot` and `daily_feedme_pull` jobs iterate `_DEV_TENANTS` dict in `auth.py`. This hardcoded dict breaks multi-tenant scale.

- [ ] Replace `_DEV_TENANTS` iteration with DB query: `SELECT id, slug FROM tenants WHERE active = true`
- [ ] Add `active` column to tenants table
- [ ] Ensure PDPA privacy policy is updated before any non-Hakshan tenant onboards

---

## Prerequisite: PDPA Compliance Before Multi-Tenancy

**Status:** Blocking E2 (shareholder link) for all tenants.

- [ ] Get written decision from Hakshan CEO/COO on shareholder data access (required before E2 implementation)
- [ ] Update BrandPulse privacy policy to cover shareholder read-only link data exposure
- [ ] Legal review before onboarding tenants outside Hakshan corporate group

---

## Tech Debt: Rename run_weekly_snapshot()

Low-priority rename — won't break anything if deferred.

- [ ] Rename `run_weekly_snapshot()` → `run_daily_snapshot()` in snapshot_service.py
- [ ] Update all callers: `app.py` (2 references), APScheduler job definition

---

## Observability: Railway Alerting Setup

- [ ] Configure Railway log drain to Datadog or Better Uptime
- [ ] Set up alert on `level=ERROR, alert=true` structured log events (FeedMe failures, digest failures)
- [ ] Set up weekly uptime check on `/t/hakshan/dashboard`

---

## Future: Key Rotation Documentation

- [ ] Document rotation procedure for `TENANT_SECRET_KEY` (Fernet encryption key)
- [ ] Document rotation procedure for `SHARE_HMAC_KEY` (shareholder token signing key) — rotating this invalidates all active shareholder links; notify shareholders before rotating
- [ ] Add key rotation runbook to ops documentation
