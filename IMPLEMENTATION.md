# BrandPulse Unified Intelligence Dashboard — Implementation Task List

Generated by /plan-ceo-review SCOPE EXPANSION on 2026-05-24
Branch: claude/check-gstack-skill-LkoC7

---

## Phase 0: Prerequisites (No code until both are resolved)

**P0-1. FeedMe API spike — confirm per-branch vs chain-level response**
- Make one successful authenticated API call that returns sales data for a single day.
- Files: none (exploratory spike; findings captured as a comment at top of `feedme_service.py`)
- Constraint: All `feedme_service.py` contracts are provisional until this resolves whether `branch_id` will be populated in `sales_snapshots`. If chain-level only, E5 degrades to `50% rating + 50% review_velocity`.
- Effort: XS

**P0-2. Shareholder PDPA written decision**
- Obtain written decision (email or memo) from Hakshan CEO or COO — NOT Brandon — confirming: (a) data categories exposed, (b) shareholders are authorized recipients under PDPA Malaysia, (c) privacy policy will be updated before distribution.
- Files: none (document artifact, not code; gates E2 entirely)
- Effort: XS (admin task)

---

## Phase 1: Foundation (infrastructure, migrations, scheduler fix)

**1-1. Add requirements for new dependencies**
- Add `anthropic>=0.26.0` and confirm `apscheduler>=3.10.0` in `requirements.txt`.
- File: `requirements.txt`
- Effort: XS

**1-2. Add `SHARE_HMAC_KEY` to env var registry**
- Add `SHARE_HMAC_KEY` to `_OPTIONAL_ENV_VARS` in `app.py` with description "Signs shareholder read-only tokens — required when E2 ships". Move to `_REQUIRED_ENV_VARS` when E2 is implemented. Do NOT use `TENANT_SECRET_KEY` for HMAC — separate keys, separate purposes.
- File: `app.py`
- Effort: XS

**1-3. Write migration `003_sales_snapshots.sql`**
- Create `sales_snapshots` table with: `id uuid`, `tenant_id text`, `branch_id text` (nullable), `snapshot_date date`, `total_revenue numeric(12,2)`, `transaction_count integer`, `created_at timestamptz`. Constraints: `UNIQUE(tenant_id, branch_id, snapshot_date)` for non-NULL rows; partial unique index `ON sales_snapshots (tenant_id, snapshot_date) WHERE branch_id IS NULL` for chain-level rows; lookup index `ON sales_snapshots (tenant_id, snapshot_date DESC)`. Enable RLS.
- File: `migrations/003_sales_snapshots.sql` (new)
- Constraint: PostgreSQL NULL != NULL — the UNIQUE constraint does NOT prevent duplicate chain-level rows; the partial index is mandatory.
- Effort: S

**1-4. Write migration `004_intelligence_digests.sql`**
- Create `intelligence_digests` table: `id uuid`, `tenant_id text NOT NULL`, `week_start date NOT NULL`, `digest_text text` (nullable), `generated_at timestamptz DEFAULT now()`. `UNIQUE(tenant_id, week_start)`. Enable RLS.
- File: `migrations/004_intelligence_digests.sql` (new)
- Effort: XS

**1-5. Fix scheduler startup — move into `create_app()`**
- Move `setup_scheduler(app)` call to the last line inside `create_app()` before `return app`. Remove from `if __name__ == "__main__":` block. Remove the `try/finally scheduler.shutdown()` from `__main__`.
- File: `app.py`
- Constraint: CRITICAL (S1) — on Railway/Gunicorn `__main__` is never set; the scheduler never starts without this fix. All APScheduler jobs depend on it.
- Effort: XS

**1-6. Upgrade APScheduler to SQLite persistent jobstore (E7)**
- In `setup_scheduler()`, replace `BackgroundScheduler(daemon=True)` with `BackgroundScheduler(jobstores={'default': SQLAlchemyJobStore(url='sqlite:///scheduler.db')}, executors={'default': ThreadPoolExecutor(max_workers=2)}, job_defaults={'misfire_grace_time': 3600})`. Add `replace_existing=True` to every `scheduler.add_job()` call.
- File: `app.py`
- Constraint: SQLite jobstore = crash recovery within deployment. `replace_existing=True` = handles Railway ephemeral disk wipe on redeploy. Both are required.
- Effort: XS

**1-7. Fix `snapshot_date` timezone to MYT + rename `run_weekly_snapshot()`**
- In `snapshot_service.py`: (a) Replace `date.today()` with `from zoneinfo import ZoneInfo; datetime.now(ZoneInfo('Asia/Kuala_Lumpur')).date()` in `store_snapshot()`. (b) Rename `run_weekly_snapshot()` → `run_daily_snapshot()`. Update 2 callers in `app.py`.
- Files: `snapshot_service.py`, `app.py`
- Constraint: S4 — using UTC causes off-by-one date (4am MYT = 8pm UTC previous day); breaks Monday digest's Sunday-data check.
- Effort: XS

**1-8. Add `get_snapshot_history()` to `snapshot_service.py`**
- Add `get_snapshot_history(tenant_id: str, branch_id: str, weeks: int = 8) -> list[dict]`. Queries `review_snapshots` ordered by `snapshot_date DESC`, limited to `weeks` rows. Returns `[{"snapshot_date": date, "rating": float, "review_count": int}]`. File-based fallback included.
- File: `snapshot_service.py`
- Effort: S

---

## Phase 2: FeedMe Integration

**2-1. Create `feedme_service.py` — `fetch_feedme_sales()` stub**
- PROVISIONAL until P0-1 spike. Implement stub `fetch_feedme_sales(tenant_id: str, date: date) -> list[dict]`. Gets FeedMe API key via `decrypt_tenant_secret(tenant_id, 'feedme_api_key')`. Normalizes to `[{"branch_id": str | None, "snapshot_date": date, "total_revenue": Decimal, "transaction_count": int}]`.
- File: `feedme_service.py` (new)
- Constraint: Comment `# PROVISIONAL — see P0-1 spike results` at top. FeedMe key in `tenant_credentials` table (Fernet-encrypted), NOT env var.
- Effort: S

**2-2. Add `decrypt_tenant_secret()` to `auth.py`**
- `decrypt_tenant_secret(tenant_id: str, key_name: str) -> str | None`. Reads encrypted column from `tenant_credentials` table, decrypts using `TENANT_SECRET_KEY` via Fernet.
- File: `auth.py`
- Effort: S

**2-3. Implement `store_feedme_sales()` in `feedme_service.py`**
- `store_feedme_sales(tenant_id: str, rows: list[dict]) -> None`. Upserts into `sales_snapshots`. Per-branch: conflict on `(tenant_id, branch_id, snapshot_date)`. Chain-level: conflict on partial index `sales_snapshots_chain_level`.
- File: `feedme_service.py`
- Effort: S

**2-4. Implement `run_daily_feedme_pull()` with retry logic**
- `run_daily_feedme_pull(tenant_id: str, snapshot_date: date | None = None) -> dict`. Computes MYT yesterday if no date passed. Retries 3x at 15-minute intervals. On HTTP 429: check `Retry-After` header, wait that duration or 300s default. On all-3-failed: log `level=ERROR, alert=true, event=feedme_pull_failed`.
- File: `feedme_service.py`
- Constraint: `snapshot_date` override enables manual backfill (success criterion 4).
- Effort: S

**2-5. Register `daily_feedme_pull` APScheduler job at 04:00 MYT (20:00 UTC)**
- Add job to `setup_scheduler()`: `trigger='cron', hour=20, minute=0, id='daily_feedme_pull', replace_existing=True`. Wrap in Flask app context.
- File: `app.py`
- Effort: XS

**2-6. Add admin endpoint `POST /api/feedme/backfill`**
- Validates `ADMIN_TOKEN`. Accepts `?tenant=<slug>&date=<YYYY-MM-DD>`. Calls `run_daily_feedme_pull(tenant_id, snapshot_date=date)`. Required for success criterion 4 gap recovery.
- File: `app.py`
- Effort: XS

---

## Phase 3: Intelligence Layer

**3-1. Implement `compute_all_health_scores()` in `snapshot_service.py` (E5)**
- `compute_all_health_scores(tenant_id: str, branches_data: list[dict]) -> dict[str, int | None]`. Formula: `40% * rating_normalized + 30% * review_velocity_normalized + 30% * sales_trend_normalized`. Priority order: (1) return None if both review_velocity and sales_trend unavailable; (2) degrade to 50/50 if sales unavailable chain-wide; (3) treat min==max component as 0.5. Returns `{branch_id: score_or_None}`.
- File: `snapshot_service.py`
- Constraint: S7 — batch function. Dashboard route passes pre-fetched data; do NOT query per-branch in a loop.
- Effort: S

**3-2. Create `digest_service.py` with `build_intelligence_digest()`**
- `build_intelligence_digest(tenant_id: str, week_start: date, branches: list[dict]) -> str | None`. Model: `claude-sonnet-4-6`, `max_tokens=250`. Skip if < 2 branches with complete data (return None, log INFO). On 5xx: retry once after 30s. On Claude refusal (response < 20 words): do NOT retry, store NULL, log `level=WARNING, event=digest_refusal`. On second 5xx failure: return None, log `level=ERROR, alert=true, event=digest_generation_failed`.
- File: `digest_service.py` (new)
- Effort: M

**3-3. Add `store_intelligence_digest()` and `get_latest_digest()` to `digest_service.py`**
- `store_intelligence_digest(tenant_id, week_start, digest_text)` — upserts, NULL is a valid value. `get_latest_digest(tenant_id) -> dict | None` — returns latest row by `week_start DESC`.
- File: `digest_service.py`
- Effort: S

**3-4. Implement `run_weekly_digest()` in `digest_service.py`**
- Computes MYT Monday `week_start`. Checks for Sunday `sales_snapshots` row — if missing, stores NULL and logs ERROR. Fetches branch data, computes `review_delta` per branch, calls `build_intelligence_digest()`, calls `store_intelligence_digest()`. NULL from API failure = `level=ERROR, alert=true`. NULL from insufficient data = `level=INFO`.
- File: `digest_service.py`
- Effort: S

**3-5. Register `weekly_digest` APScheduler job at 05:30 MYT Mondays (21:30 UTC)**
- Add job: `trigger='cron', day_of_week='mon', hour=21, minute=30, id='weekly_digest', replace_existing=True`.
- File: `app.py`
- Effort: XS

**3-6. Extend `tenant_dashboard` route to fetch and pass new data**
- Query `sales_snapshots` (all branches, selected date), `get_latest_digest()`, call `compute_all_health_scores()` with pre-fetched data. Add `?date=YYYY-MM-DD` param handling. Pass all to template.
- File: `app.py`
- Constraint: Single query for all branch data — no per-branch loops.
- Effort: S

---

## Phase 4: E4/E5/E6 Polish

**4-1. E4: Update branch card grid to `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`**
- Change from `md:grid-cols-2 xl:grid-cols-3`. Ensure all buttons `min-h-[44px]`.
- File: `templates/tenant/dashboard.html`
- Constraint: Test at 390px (iPhone 16). No horizontal overflow.
- Effort: XS

**4-2. E5: Add health score badge to each branch card**
- Green (≥70), amber (50–69), red (<50). If score is None, hide badge entirely. `title="How is this calculated?"` tooltip.
- File: `templates/tenant/dashboard.html`
- Effort: XS

**4-3. E6: Add `compute_review_velocities()` to `snapshot_service.py`**
- `compute_review_velocities(snapshots: list[dict]) -> list[int | None]`. For pairs > 8 days apart: insert None (gap signal for sparkline break).
- File: `snapshot_service.py`
- Effort: S

**4-4. E6: Add sparkline SVG macro to `dashboard.html`**
- 64×24px inline SVG. Green stroke (positive), gray (flat), red (negative). None velocity = SVG `M` (moveto) for gap break. Empty state: gray rectangle.
- File: `templates/tenant/dashboard.html`
- Effort: S

---

## Phase 5: E2 — Shareholder Link (gated behind P0-2 PDPA decision)

**5-1. Create `share_bp` Flask Blueprint with no auth middleware**
- New file `share_routes.py` with blueprint. Register in `create_app()`. NO login `before_request` hook.
- Files: `share_routes.py` (new), `app.py`
- Effort: XS

**5-2. Implement HMAC token generation and validation**
- `generate_share_token(tenant_id, expiry_days=30) -> str`. `validate_share_token(token, payload) -> bool`. Uses `SHARE_HMAC_KEY`. Constant-time comparison via `hmac.compare_digest`. Payload: `f"{tenant_id}:{exp_unix_timestamp}"`.
- File: `share_routes.py`
- Effort: S

**5-3. Implement `GET /t/<slug>/share/<token>` and stripped template**
- Validates token, queries digest + health scores + 4-week sparkline. Renders `templates/share/dashboard.html` — AI digest, health badges, revenue sparkline. No PII. "Data as of [date]" label.
- Files: `share_routes.py`, `templates/share/dashboard.html` (new)
- Effort: M

**5-4. Add `POST /t/<slug>/share/generate` admin endpoint**
- Admin-only (ADMIN_TOKEN). Returns `{"url": "https://.../t/<slug>/share/<token>"}`. Stateless HMAC — no DB storage.
- File: `share_routes.py`
- Effort: XS

---

## Phase 6: Dashboard Redesign

**6-1. Add "Commercial Performance" section**
- Revenue table: Branch | Revenue MYR | Transactions | vs Last Week. Date picker (`?date=`). `overflow-x-auto` wrapper. Placeholder if no `sales_snapshots` yet.
- File: `templates/tenant/dashboard.html`
- Effort: S

**6-2. Add "Intelligence" section**
- "Week of [Monday date]" label, digest paragraph, `generated_at` timestamp. Fallback: "Weekly digest unavailable — check back Monday." Use `{% if digest.digest_text %}` guard.
- File: `templates/tenant/dashboard.html`
- Effort: XS

**6-3. Update hero stats to include chain-level revenue**
- Add "Weekly Revenue (MYR)" stat card. Confirm with Brandon which existing card to replace (suggestion: Facebook Likes).
- File: `templates/tenant/dashboard.html`
- Effort: XS

---

## Summary

| Phase | Tasks | Effort |
|---|---|---|
| 0: Prerequisites | P0-1, P0-2 | XS + admin |
| 1: Foundation | 1-1 through 1-8 | ~3S + 5XS |
| 2: FeedMe | 2-1 through 2-6 | ~4S + 2XS |
| 3: Intelligence | 3-1 through 3-6 | 1M + 4S + 1XS |
| 4: Polish | 4-1 through 4-4 | 2S + 2XS |
| 5: E2 Shareholder | 5-1 through 5-4 | 1M + 2S + 1XS |
| 6: Dashboard | 6-1 through 6-3 | 1S + 2XS |

**Key ordering constraints:**
- P0-1 before 2-1 (FeedMe API shape determines function contracts)
- P0-2 before anything in Phase 5
- 1-5 (scheduler startup fix) before 2-5 and 3-5 (job registration useless without it)
- 1-3, 1-4 (migrations) applied to Supabase before 2-3, 3-3, 3-6 touch those tables
- 3-6 (route fetches data) before 4-2, 4-4, 6-1, 6-2, 6-3 (template renders it)
